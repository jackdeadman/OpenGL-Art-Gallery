public interface HandPosition {
    // public HandConfiguration getConfiguration();

    // 0.5 == in position, afterwards it returns back to neutral
    // public HandConfiguration Tween(float t);
}
